// Placeholder. Served as-is when the page is opened from a plain web server
// (GitHub Pages, python -m http.server), where there is no desktop theme to
// follow and the page offers its own six schemes instead.
//
// Under the ENCOM Boardroom server this file never reaches the browser: that
// server answers /palette.js itself, with the current theme's encom.json, and
// the page then draws the duel in the theme the desktop is wearing.
